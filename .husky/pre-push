#!/usr/bin/env sh
if [ "${CI:-}" = "true" ] || [ "${GITHUB_ACTIONS:-}" = "true" ]; then
  exit 0
fi
set -e

cd "$(git rev-parse --show-toplevel)"

echo "pre-push: format, typecheck, test…"
bun run format:check
bun run typecheck
bun run test
