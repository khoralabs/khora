#!/usr/bin/env sh
if [ "${CI:-}" = "true" ] || [ "${GITHUB_ACTIONS:-}" = "true" ]; then
  exit 0
fi
set -e

cd "$(git rev-parse --show-toplevel)"

echo "pre-commit: Biome check (run 'bun run format' to fix)…"
bun run format:check

echo "pre-commit: typecheck…"
bun run typecheck
