name: publish khora-cli skills

# Separate from release-khora-cli so a missing PAT or publish failure cannot block npm/Homebrew.
on:
  workflow_dispatch:
    inputs:
      version:
        description: "Semver stamped into skill-source.json (match the released CLI)"
        required: true
        type: string
      ref:
        description: "Git ref to publish from (branch, tag, or SHA). Default: workflow ref."
        required: false
        type: string
        default: ""

permissions:
  contents: read

jobs:
  publish:
    runs-on: blacksmith-4vcpu-ubuntu-2404
    env:
      VERSION: ${{ inputs.version }}
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
          ref: ${{ inputs.ref != '' && inputs.ref || github.ref }}

      - uses: ./.github/actions/checkout-khora
        with:
          token: ${{ secrets.KHORALABS_SUBMODULES_TOKEN }}

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: install
        run: bun install --frozen-lockfile

      - name: test publish-skills
        run: bun test scripts/release/publish-skills.test.ts

      - name: verify skills-repo token
        env:
          GH_TOKEN: ${{ secrets.SKILLS_REPO_TOKEN }}
        run: |
          set -euo pipefail
          if [ -z "${GH_TOKEN:-}" ]; then
            echo "::error::SKILLS_REPO_TOKEN is empty or unset"
            exit 1
          fi
          push="$(gh api repos/khoralabs/skills --jq '.permissions.push // false')"
          if [ "$push" != "true" ]; then
            echo "::error::SKILLS_REPO_TOKEN lacks push permission on khoralabs/skills"
            exit 1
          fi
          echo "SKILLS_REPO_TOKEN can push to khoralabs/skills"

      - name: publish skills
        env:
          SKILLS_REPO_TOKEN: ${{ secrets.SKILLS_REPO_TOKEN }}
        run: bun run scripts/release/publish-skills.ts "$VERSION"
