# Bun image with jq for shell JSON parsing in demo scripts.
FROM oven/bun:1
RUN apt-get update \
  && apt-get install -y --no-install-recommends jq \
  && rm -rf /var/lib/apt/lists/*
WORKDIR /app
COPY . .
RUN bun install --frozen-lockfile
