/** Primary demo namespace (subtree root for search). */
export const DEMO_NS = "demo";

/** Second namespace for multi-namespace search demos. */
export const DEMO_NS_B = "demo-b";

export const SEARCH_TOP_K = 20;

/** Vector width used by `demoVector768` (must be in Convex-supported dimensions). */
export const DEMO_VECTOR_DIM = 768 as const;
