# @khoralabs/relay-colonnade
#
# This package does not read process.env. Wire these from your host’s env into
# createRelayColonnadeSocial / catalog adapters, e.g.:
#   { catalogPath: envCatalogPath(), framesDbPath: envFramesDbPath(), tenantKey?: envTenantKey() }
#
# Paths must stay consistent with @khoralabs/colonnade-persistence / Khora:
# catalogPath  → same file as ATRIUM_CATALOG_PATH
# framesDbPath → Khora frames / frame-channel DB (not the cells directory)

# --- Catalog SQLite (shared with colonnade-persistence) ---
ATRIUM_CATALOG_PATH=./data/khora-catalog.sqlite

# --- Frames / frame-channel SQLite (relay-colonnade social layer) ---
ATRIUM_FRAMES_DB_PATH=./data/khora-frames.sqlite

# --- Optional relay tenant key prefix in catalog keys (library default: "relay") ---
# ATRIUM_RELAY_TENANT_KEY=

# --- Same-process Khora / colonnade (not read by this package; for one-file deploy docs) ---
# ATRIUM_CELLS_DIR=./data/cells
# ATRIUM_CELL_POOL_COUNT=16
