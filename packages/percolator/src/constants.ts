/** Default minScore when neither create.minScore nor search.options.minScore is set. */
export const DEFAULT_MIN_SCORE = 0;

/** Fixed score for filter-only matches when all search filters pass. */
export const FILTER_ONLY_MATCH_SCORE = 1;
