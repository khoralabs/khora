# Umami analytics (see apps/umami/README.md).
# Requires DATABASE_URL and APP_SECRET — set in .env (local) or Render env vars (production).
# Set DISABLE_BOT_CHECK=1 to accept server-side events forwarded from Exedra.
FROM docker.umami.is/umami-software/umami:postgresql-latest

EXPOSE 3000
