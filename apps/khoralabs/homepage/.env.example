# Copy to .env. Bun loads .env automatically when you `bun run dev`.

PORT=3000

# Khora registry — access-token requests go directly to the registry service
KHORA_REGISTRY_URL=http://localhost:4000
BUN_PUBLIC_KHORA_REGISTRY_URL=http://localhost:4000
