# Disk-backed persistence for waitlist KV (homepage Bun.redis).
dir /data
appendonly yes
appendfsync everysec

# Optional password: set REDIS_PASSWORD in docker run; entrypoint substitutes via env.
# requirepass is set at runtime when REDIS_PASSWORD is non-empty (see README).
