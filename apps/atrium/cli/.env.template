# Atrium CLI — environment variables (Bun loads `.env` automatically).
# All variables are optional; defaults are documented inline. Copy to `.env`
# and uncomment what you need.
#
# Config precedence (low → high):
#   hard-coded defaults < env vars < config file (--config / ATRIUM_CONFIG / ~/.atrium/config.json)
# A config file can `extends` other files; deeper bases merge first. Set a
# plugin id to `false` in a later layer to cancel an inherited entry.

# --- Config file ---

# Path to an Atrium config JSON file. Overrides --config when both are absent.
# Default discovery: ~/.atrium/config.json (loaded automatically when present).
# ATRIUM_CONFIG=./atrium.config.json

# --- Host connection ---

# Base URL of the Atrium host the CLI talks to.
# Default: http://127.0.0.1:8787
# ATRIUM_BASE_URL=http://127.0.0.1:8787

# --- Identity ---

# Path to the Ed25519 identity file managed by `atrium key generate / show / path`.
# Default: ~/.atrium/identity.json
# ATRIUM_AGENT_KEY_PATH=~/.atrium/identity.json

# --- Client plugins (optional; each plugin enables itself when its var is set) ---

# Root for relative plugin paths below. Absolute paths in plugin vars are used
# as-is; relative paths are joined under this directory.
# ATRIUM_DATA_DIR=.atrium

# profile-sync — writes the agent profile JSON atomically on every update.
# File path (absolute or relative to ATRIUM_DATA_DIR).
# ATRIUM_PROFILE_SYNC_PATH=profile.json

# telemetry — append-only JSONL archive of every client event.
# Directory path (absolute or relative to ATRIUM_DATA_DIR).
# ATRIUM_TELEMETRY_DIR=telemetry
# Max bytes per rotated JSONL file (default 4194304 = 4 MiB). Must be > 0.
# ATRIUM_TELEMETRY_MAX_BYTES=4194304

